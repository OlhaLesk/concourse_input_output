platform: linux
image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
  - name: resource-tutorial
  - name: some-files

outputs:
  - name: test-output

run:
  path: resource-tutorial/show_files.sh
